spec:
  name: dreamboard-do
  services:
    - build_command: yarn build
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/alexxozo/dreamboard-do.git
      name: dreamboard-do
